# image2arduino
Arduino image-to-byte-array converter — browser-based tool that turns PNG/JPG/BMP images into C header files ready for use with TFT displays (RGB565), OLEDs (SSD1306), and other embedded targets.
